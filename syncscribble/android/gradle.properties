# AGP 8.4 predates compileSdk 36 (the platform baked into the r/android image);
# the combination builds fine, this just silences the untested-compileSdk warning.
android.suppressUnsupportedCompileSdk=36
